<div class="container-md">
  <div class="row justify-content-center">
    <div class="cellbody">
      <div class="flex-container background-home">
        <div class="align-center app-name pt-1 pb-2">
          <br>
          <h3>Voz<br>das<br>escolas</h3>
        </div>
        <br>
        <br>
        <br>
        <div class="flex-item app-slogan">
          <h5>Compartilhe os problemas da sua escola! Agora sua voz será ouvida.</h5>
        </div>
        <div class="flex-item align-center">
          <% if user_signed_in? %>
            <%= link_to "Informe um problema", new_school_report_path(current_user.school_id), class: "btn btn-info" %>
          <% else %>
            <%= link_to "Informe um problema", new_user_session_path, class: "btn btn-info" %>
          <% end %>
        </div>
        <br>
        <div class="flex-item align-center">
          <% if user_signed_in? %>
            <%= link_to "Veja os problemas da sua escola", school_reports_path(current_user.school_id), class: "link-escola" %>
          <% else %>
            <%= link_to "Veja os problemas da sua escola", new_user_session_path, class: "link-escola" %>
          <% end %>
          <br>
          <br>
          <br>
          <br>
        </div>
      </div>
    </div>
  </div>
</div>
