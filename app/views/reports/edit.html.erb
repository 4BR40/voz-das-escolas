<div class="container-md">
  <div class="row justify-content-center">
    <div class="cellbody col-md-5">
      <h1>Altere seu relato</h1>
    </div>
  </div>
</div>
<div class="container-md">
  <div class="row justify-content-center">
    <div class="cellbody col-md-5 pb-5">
      <%= simple_form_for @report do |f| %>
        <%= f.input :description, as: :text, label:"1. Altere a descrição do problema:" %>
        <%= image_tag "", width: 300, class: "hidden", id: "photo-preview" %>
        <p>2. Caso queira alterar as imagens postadas, insira novas aqui:</p>
        <div class="gallery" ></div>
        <%= f.input :photos, as: :file, input_html: { multiple: true, class: 'hidden', id: 'photo-input'}, label_html: { class: 'upload-photo'}, label: '📷 Faça o upload' %>
        <p>3. Caso queira alterar a categoria do problema, escolha abaixo:</p>
        <%= f.association :category,
            as: :radio_buttons,
            collection_wrapper_tag: 'div',
            collection_wrapper_class: 'category-wrapper',
            item_wrapper_class: 'category-item',
            input_html: {class: 'category-selector'},
            legend_tag: false %>
        <p>4. Desmarque a opção abaixo apenas se não quiser que seu relato fique visível para sua escola: </p>
        <%= f.input :public, label: 'Público', input_html: { class: "toggle" } %>
        <br>
        <p>5. Marque abaixo para declarar o problema como <strong>resolvido</strong>:</p>
        <%= f.input :solved, label: 'Resolvido!', input_html: { class: "toggle" } %>
        <br>
        <%= f.button :submit, 'Alterar relato', class: 'btn btn-info btn-block' %>
      <% end %>
    </div>
  </div>
</div>
